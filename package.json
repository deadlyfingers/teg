{
  "name": "tegraham",
  "version": "0.0.1",
  "private": true,
  "description": "HTML web page",
  "scripts": {
    "test": "echo \"Error: no test specified\"",
    "clean": "rm -r build",
    "build": "npm run clean && mkdir build && cp index.html build && cp CNAME build",
    "deploy": "gh-pages -d build"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "gh-pages": "^2.2.0"
  },
  "homepage": "https://www.tegraham.com"
}
